# KeePassXC 使用教程

写给自己看的实操教程。不讲原理，只讲怎么做。

本教程基于 **KeePassXC 2.7.12 中文界面**。所有菜单路径和快捷键都在该版本上实测过（快捷键另经 2.7.12 源码 `src/gui/MainWindow.cpp` 核实）。

- [一、第一次使用：建自己的密码库](#一第一次使用建自己的密码库)
- [二、日常就三个操作](#二日常就三个操作)
- [三、同一个网站/软件挂多个账号](#三同一个网站软件挂多个账号)
- [四、备份（最重要的一节）](#四备份最重要的一节)
- [五、浏览器里自动填密码](#五浏览器里自动填密码)
- [六、桌面软件自动输入](#六桌面软件自动输入)
- [七、手机上用](#七手机上用)
- [八、快捷键速查](#八快捷键速查)
- [九、常见坑](#九常见坑)

---

## 一、第一次使用：建自己的密码库

### 1. 建库

菜单 **数据库 → 新建数据库**，然后一路往下：

| 步骤 | 怎么填 |
|---|---|
| 数据库名称 | 随便，比如「我的密码库」 |
| 加密设置 | **密钥派生函数选 `Argon2id`**。默认通常已经是，确认一下。旁边有「基准测试」按钮，点它会按你这台机器的性能自动调参数到约 1 秒 |
| 数据库凭据 | 输主密码（下一节细说） |
| 保存位置 | 选一个**不会被你随手清理**的目录 |

### 2. 关于主密码

这是唯一一个你必须自己记住的密码。要求只有一条：**够长**。

- 推荐：一句只有你懂的话，例如 `我家猫2019年偷吃了3条鱼`
- 不推荐：`Xk9#mP2q` 这种——又难记，长度又不够

> ⚠️ **主密码丢了，库就永久打不开。没有找回，没有客服，没有任何人能帮你。**
> 头一两周建议把它写在纸上锁进抽屉，等形成肌肉记忆后再销毁。

### 3. 关于保存位置

**别存在桌面相关的目录里**，哪天整理桌面手一抖就删了。建一个专门的目录，例如：

```
D:\密码库\我的密码库.kdbx
```

### 4. 关于「密钥文件」双因子（可选）

除了主密码，再加一个小文件当第二把钥匙，两个都得有才能开库。

- **好处**：别人光偷到你的主密码也打不开
- **坏处**：那个文件丢了，库同样永久报废，必须单独备份，且不能和库文件放在同一个地方（否则等于没加）

新手建议先不用，只用一个足够长的主密码。等流程熟了再考虑。

---

## 二、日常就三个操作

### 存一个新账号

```
Ctrl + N  →  填 标题 / 用户名 / 密码 / URL  →  确定  →  Ctrl + S 保存
```

密码那一栏右边有个**骰子图标**，点它自动生成强密码，不用自己想。生成的密码长度和字符集可以调。

**URL 一定要填**，浏览器自动填充靠它匹配网站。

### 取一个密码来用

```
Ctrl + F 搜索  →  选中条目  →  Ctrl + C 复制密码
```

- `Ctrl + B` 复制用户名
- `Ctrl + U` 复制 URL
- `Ctrl + Shift + U` 直接用浏览器打开该网站

复制后剪贴板会**自动清空**，不会一直留着。时长在 `工具 → 设置 → 安全` 里可调。

### 改完一定要保存

标题栏出现 `*` 说明有未保存的改动，`Ctrl + S` 存盘。可以在 `工具 → 设置 → 常规` 里打开「自动保存」，改完即存。

---

## 三、同一个网站/软件挂多个账号

这是最常见的需求，KeePassXC 里**没有任何数量限制**——同一个 URL 建多少条目都行。

### 最快的做法：克隆

选中已有的那条，按 **`Ctrl + K` 克隆**，然后改用户名和密码。URL、分组、图标全都自动跟着复制，不用重填。

### 推荐的命名方式

标题里带上区分信息，一眼能认出来：

```
网站/
├─ GitHub (主号)        用户名 me@example.com      URL https://github.com
├─ GitHub (机器人)      用户名 bot-01           URL https://github.com
├─ GitHub (公司)        用户名 work@example.com    URL https://github.com
├─ 淘宝 (主号)          用户名 138****0000      URL https://taobao.com
└─ 淘宝 (小号)          用户名 alt_buyer        URL https://taobao.com

桌面软件/
├─ Steam (国区)         用户名 cn_player
├─ Steam (美区)         用户名 us_player
└─ Steam (小号)         用户名 smurf_acc
```

**关键点：URL 保持完全相同。** 标题用来给人看，URL 用来给浏览器插件匹配。

### 怎么快速找出某站点的所有账号

`Ctrl + F` 搜网站名，结果列表会列出全部匹配条目，并带上「群组」和「用户名」两列，一眼分清是哪个号。

非网站的软件（Steam、各种客户端）同样这么管，只是 URL 那栏可以留空或填官网。

---

## 四、备份（最重要的一节）

**这是本地方案唯一真正的风险点。** 库文件只有一个，硬盘坏了、误删了、清空回收站了——密码全没，没人能救。

### 必做：异地拷贝

把 `.kdbx` 文件复制到**另一个物理位置**：U 盘、另一块硬盘、或者网盘。

**文件本身是加密的，扔网盘不怕被网盘公司看到**（前提是主密码够长）。可以用命令验证这一点：文件里搜不到任何明文账号。

```bash
grep -a "你的某个用户名" 我的密码库.kdbx    # 应该什么都搜不到
```

### 顺手打开：保存前自动备份

`工具 → 设置 → 常规` → 勾上「**保存前备份数据库**」。

之后每次保存，KeePassXC 会在同目录留一份上一版的副本。这能救「改错了想回退」，但**救不了硬盘坏**——异地拷贝还是得你自己做。

### 建议的备份目录结构

把程序和库放在一起，换机器时直接能用：

```
U盘 或 备份硬盘\
├─ 我的密码库.kdbx                      ← 你的密码
├─ 我的密码库.kdbx.old                  ← 自动备份副本
└─ 程序\
   ├─ KeePassXC-2.7.12-Win64.zip        ← 便携版，解压双击就能用
   └─ KeePassDX-4.4.5-libre.apk         ← 安卓版
```

程序都可以从本仓库的 [Releases](../../releases) 下载。

---

## 五、浏览器里自动填密码

### 安装

1. 浏览器装扩展 **KeePassXC-Browser**（Chrome / Edge 应用商店、Firefox 附加组件都有）
2. KeePassXC 里 `工具 → 设置 → 浏览器集成` → 勾选「启用浏览器集成」，再勾上你用的浏览器
3. 回到浏览器点扩展图标 → 「连接」，KeePassXC 会弹窗要你给这个连接起个名字，确认即可

### 用起来是什么样

打开登录页，用户名输入框右边会出现 KeePassXC 图标，点一下：

- 只有一个账号 → 直接填上
- **同一网站有多个账号 → 弹出列表让你选哪个**，这就是前面「同站多账号」在浏览器里的效果

新注册账号时，提交后扩展会问「要不要保存到 KeePassXC」，点是就自动建条目。

### 注意

- 浏览器集成需要往注册表写一条 native messaging 配置（指向 `keepassxc-proxy.exe`）。**便携版如果之后挪动了文件夹路径，这条会失效，得重新配一次**；安装版路径固定，不会有这问题
- 库处于锁定状态时扩展填不了密码，需要先解锁 KeePassXC

---

## 六、桌面软件自动输入

Steam、各种客户端这类没有网页的程序，用「自动输入」。

### 怎么用

1. 把目标软件的登录窗口打开，放在前台
2. 切到 KeePassXC，选中对应条目
3. 按 **`Ctrl + Shift + V`**

KeePassXC 会切回刚才那个窗口，自动敲入 `用户名` + `Tab` + `密码` + `回车`。

### 让它认得是哪个窗口

编辑条目 → 左侧「**自动输入**」→ 添加一个「窗口关联」，填目标窗口标题的关键词（支持 `*` 通配），例如 `*Steam*`。

配好之后可以用**全局快捷键**：在 `工具 → 设置 → 自动输入` 里设一个全局热键，之后在任意窗口按下它，KeePassXC 会自动匹配当前窗口标题并填入对应条目。**同一软件多个账号时会弹出列表让你选。**

### 序列可以自定义

默认序列是 `{USERNAME}{TAB}{PASSWORD}{ENTER}`。有的软件需要两次 Tab、或者不需要回车，就在条目的「自动输入」页里改成比如：

```
{USERNAME}{TAB}{TAB}{PASSWORD}
```

---

## 七、手机上用

### 装什么

从本仓库 [Release `keepassdx-4.4.5`](../../releases/tag/keepassdx-4.4.5) 下 **`KeePassDX-4.4.5-libre.apk`**（F-Droid 构建，推荐）。两个版本的区别在那个 release 说明里写清楚了。

安装：传到手机 → 文件管理器点开 → 允许「安装未知来源应用」。

### 文件怎么弄到手机上

| 方式 | 说明 |
|---|---|
| **手动拷贝** | USB 线、微信文件传输助手。最简单，但每次改完都得重传。适合手机上只是偶尔查 |
| **网盘** | 阿里云盘 / 百度网盘。文件加密，不怕泄露，但这两家不支持 WebDAV，同步不够顺 |
| **Syncthing（推荐）** | 开源点对点同步，**局域网直连，完全不经过任何云**。手机连上家里 WiFi 就自动双向同步。国内可用，不需要梯子 |

### 手机上的自动填充

1. 打开 KeePassDX，选择你的 `.kdbx`，输主密码
2. 进设置开启**指纹/面容解锁**，之后不用每次打字
3. 系统设置里把 KeePassDX 设为**自动填充服务**

之后无论是 App 登录还是 Chrome 里登录，输入框上方会弹出账号列表——**同一网站存了三个账号就列出三个让你点**，和电脑上一致。

> ⚠️ **别在电脑和手机上同时改同一个库。** 同步工具处理不了这种冲突：Syncthing 会生成 `.sync-conflict` 文件，你得回 KeePassXC 里用「数据库 → 合并数据库」手动处理。养成「一次只在一个地方改」的习惯就没事。

---

## 八、快捷键速查

以下均核实自 KeePassXC 2.7.12 源码 `src/gui/MainWindow.cpp` 与实际界面。

### 最常用

| 快捷键 | 作用 |
|---|---|
| `Ctrl + F` 或 `F3` | 搜索（跨全库） |
| `Esc` | 清空搜索 |
| `Ctrl + N` | 新建条目 |
| `Ctrl + E` 或 `Enter` | 编辑条目 |
| `Ctrl + K` | **克隆条目**（同站加第二个账号最快的方式） |
| `Del` | 删除条目 |
| `Ctrl + B` | 复制用户名 |
| `Ctrl + C` | 复制密码 |
| `Ctrl + U` | 复制 URL |
| `Ctrl + Shift + U` | 用浏览器打开 URL |
| `Ctrl + Shift + V` | **执行自动输入**（填到桌面软件里） |
| `Ctrl + S` | 保存 |
| `Ctrl + L` | 锁定当前库 |

### 其他

| 快捷键 | 作用 |
|---|---|
| `Ctrl + Shift + N` | 新建数据库 |
| `Ctrl + O` | 打开数据库 |
| `Ctrl + W` | 关闭数据库 |
| `Ctrl + Shift + L` | 锁定所有库 |
| `Ctrl + ,` | 设置 |
| `Ctrl + Shift + ,` | 数据库设置（改 Argon2id 参数在这里） |
| `Ctrl + Shift + R` | 数据库报告（查弱密码、重复密码） |
| `Ctrl + T` | 复制 TOTP 验证码 |
| `Ctrl + Y` | 复制密码 + TOTP |
| `Ctrl + Shift + C` | 切换密码列显示/隐藏 |
| `Ctrl + Shift + B` | 切换用户名列显示/隐藏 |
| `F1` / `F2` | 焦点切到分组树 / 条目列表 |
| `Ctrl + /` | 显示完整快捷键列表 |

> 📌 程序自带的 `share/docs/KeePassXC_KeyboardShortcuts.html` 里把「克隆条目」写成了 `Ctrl + D`，**那是文档笔误**。2.7.12 的源码和实际菜单都是 `Ctrl + K`（`Ctrl + D` 在代码里被 `Del` 覆盖了，实际不生效）。

---

## 九、常见坑

### 主密码丢了怎么办

**没办法。** 库永久打不开。这不是"有点难恢复"，是数学上不可行。所以前面反复强调纸质记录和备份。

### 换电脑了怎么办

三样东西凑齐就行：**库文件 + 主密码 + 任意一个 kdbx 客户端**。

客户端不必是 KeePassXC——kdbx 是公开标准格式，KeePass、KeePassDX、Strongbox、KeeWeb、pykeepass 都能读。**你的密码不绑在任何一个软件上。**

最省事的方式：U 盘里放便携版 + 库文件，插到任何 Windows 上双击 exe 就能用，不需要安装。

### 库文件能直接用记事本看吗

不能，全是加密的二进制。文件头是 `03 D9 A2 9A 67 FB 4B B5`（kdbx 格式标识），之后全是密文，搜不到任何明文账号。

### 用命令行 `keepassxc-cli` 建的库为什么不是 Argon2id

因为 `keepassxc-cli db-create` 建出来的是 **KDBX 3.1 格式，只支持 AES-KDF**。Argon2 需要 KDBX 4。

**正式库请用 GUI 建**（GUI 默认就是 KDBX 4 + Argon2id）。`db-edit` 也改不了 KDF，只能在 GUI 的「数据库设置 → 加密」里改。

### 校验下载的安装包时报「No such file or directory」

KeePassXC 官方的 `*.DIGEST` 文件是 **CRLF 换行**，`sha256sum -c` 会把行尾的 `\r` 当成文件名的一部分。用本仓库 release 里我转成 LF 的 `*-SHA256SUMS.txt`，或者临时处理：

```bash
tr -d '\r' < KeePassXC-2.7.12-Win64.msi.DIGEST | sha256sum -c -
```

### 便携版和安装版该用哪个

| | 便携版 | 安装版 |
|---|---|---|
| 防程序被篡改 | 差 —— 目录通常任何进程都能写，恶意软件可替换 exe | **好** —— Program Files 需管理员权限才能改 |
| 浏览器插件 | 挪动路径后失效，要重配 | 路径固定，配一次就好 |
| U 盘随身带 | 可以 | 不行 |

**长期在自己电脑上用 → 安装版。带着到处跑 → 便携版。** 两者读同一个库文件，随时可换。

### 两台设备同时改了库，冲突了

用 **数据库 → 合并数据库**，选另一份文件，KeePassXC 会按条目逐个合并。Syncthing 产生的 `.sync-conflict-xxx.kdbx` 就是拿它来处理。

预防办法：一次只在一个地方改。

### 忘了库文件放哪了

```powershell
Get-ChildItem D:\ -Recurse -Filter *.kdbx -ErrorAction SilentlyContinue | Select-Object FullName, Length, LastWriteTime
```

---

## 相关链接

- 本仓库首页说明：[README.md](README.md)
- 程序下载：[Releases](../../releases)
- 官方用户手册（程序自带）：`安装目录\share\docs\KeePassXC_UserGuide.html`
- 官方文档站：<https://keepassxc.org/docs/>
